<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/tv_clear"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="5dp"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@+id/et_search"
        android:layout_alignBottom="@+id/et_search"
        android:text="清空"
        android:visibility="gone" />

    <EditText
        android:id="@+id/et_search"
        android:hint="请输入搜索关键字，编号或姓名或手机号"
style="@style/ViewBaseStyle"
        android:layout_alignParentLeft="true"
        android:layout_toLeftOf="@+id/tv_clear" />

    <TextView
        android:id="@+id/tv_hour_percentage"
        android:text="提成：0.00"
        android:visibility="gone"
        android:layout_below="@id/et_search"
style="@style/ViewBaseStyle"
        android:layout_width="90dp"/>

    <TextView
        android:id="@+id/tv_add"
style="@style/ViewBaseStyle"
        android:layout_toRightOf="@+id/tv_hour_percentage"
        android:layout_below="@+id/et_search"
        android:text="添加" />
</RelativeLayout>